# Secrets — never commit
.env

# Python
__pycache__/
*.pyc
.venv/

# Node
node_modules/
dist/
.svelte-kit/
package-lock.json

# World-map build artifacts — the map layers + meta ARE committed; these aren't
web/public/worldmap/iconcache/
web/public/worldmap/preview.png

# mapsUpdate skill — style inputs + generated maps (not committed)
/maps/
