# Standalone edges (with an explicit `from`) attach a route to a node defined in ANY
# other file. This game needs none — every route lives inline in its source node — but the
# file is kept as the place to add cross-file edges later.
edges: []
